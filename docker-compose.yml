

services:
  servicio-validacion:
    build: .
    container_name: servicio-validacion
    ports:
      - "8020:8020"
    volumes:
      - .:/app
    env_file:
      - .env
    command: uvicorn servicio_validacion.main:app --host 0.0.0.0 --port 8020 --reload

    
